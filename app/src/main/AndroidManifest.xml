<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twenty_sixth_april">

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Twentysixthapril">
        <activity android:name=".GameChineseChessActivity"/>
        <activity android:name=".GameJumpActivity" />
        <activity android:name=".GameGoActivity" />
        <activity android:name=".GameChessActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>